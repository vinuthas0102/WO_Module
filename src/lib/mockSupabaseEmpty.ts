// Empty stub for @supabase/supabase-js to avoid bundling the real client in offline mode.
// The mock client in src/lib/mockSupabase.ts provides all needed functionality.
export function createClient() {
  return {};
}
